@using System.Linq;
@using SinglePageTest.Models;
@using System.Collections.Generic;

@model dynamic

<section>
    <ul>
        @foreach (var item in (Model.items ?? new IndexItemModel[] { }))
        {
            <li>@item.name</li>
        }
    </ul>
</section>
